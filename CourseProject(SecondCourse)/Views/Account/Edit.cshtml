@model CourseProject_SecondCourse_.Models.EditModel

<h2>Редактировать данные профиля</h2>

@using (Html.BeginForm("Edit","Account", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()

    <div>
        @Html.ValidationSummary(true, "")
        <div>
            <p>Year: @Html.EditorFor(model => model.Age)</p>
        </div>
        <div>
            <p>Surname: @Html.EditorFor(model => model.Surname)</p>
        </div>
        <div>
            <p>Name: @Html.EditorFor(model => model.Name)</p>
        </div>
        <div>
            <p>PhoneNumber: @Html.EditorFor(model => model.PhoneNumber)</p>
        </div>
        <div>
            <div class="form-group">
                @Html.Label("Image", new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    <input type="file" id="Image" name="uploadImage" />
                </div>
            </div>
            @*<fieldset>
            <div class="editor-label">Файл изображения</div>
            <div class="editor-field">
                <input type="file" name="uploadImage" />
            </div>
        </fieldset>*@
        </div>
        <div>
            <div>
                <input type="submit" value="Сохранить" />
            </div>
        </div>
        @Html.ActionLink("Изменить пароль", "ChangePassword", "Manage")
    </div>
}
